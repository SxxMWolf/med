{
  "_comment": "이 파일은 배포용 vercel.json 설정입니다. 배포 시 이 파일을 vercel.json로 복사하세요.",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://16.184.46.179/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

